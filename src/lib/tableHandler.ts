export const handleEdit = (id: string) => {
    console.log(`Id is ${id} for edit`)
}

export const handleDelete = (id: string) => {
    console.log(`Id is ${id} for delete`)
}

export const handleBorrow = (id: string) => {
    console.log(`Id is ${id} for borrow`)
}